FROM node:20-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./

RUN npm ci --omit=dev && npm cache clean --force

COPY baileys ./baileys

ENV NODE_ENV=production
ENV BAILEYS_HOST=0.0.0.0
ENV PORT=3010

EXPOSE 3010

CMD ["npm", "run", "whatsapp:baileys"]
