import { PageShell } from "@/galactic/common";
import { ProfileContent } from "./section";

const ProfilePage = () => {
  return (
    <PageShell title="Profil Saya">
      <ProfileContent />
    </PageShell>
  );
};

export default ProfilePage;
